---====== Load spawner ======---
-- Modified copy of Vynixu's DOORS Entity Spawner V2 with a built-in Jumpscare option (see the Jumpscare block below)

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/Tricot676/Doors-Modes/refs/heads/main/Modern%20Doors%20Entity%20Spawner/init.luau"))()

---====== Create entity ======---

local entity = Spawner:Create({
	Entity = {
		Name = "Smiler",
		Asset = "https://github.com/reboundplayz/Doors-Fixed-Modes/raw/refs/heads/main/Smiler.rbxm?raw=true",
		HeightOffset = 1
	},
	Lights = {
		Flicker = {
			Enabled = true,
			Duration = 5
		},
		Shatter = true,
		Repair = false
	},
	Earthquake = {
		Enabled = false
	},
	CameraShake = {
		Enabled = true,
		Range = 100,
		Values = {20, 20, 1, 2} -- Magnitude, Roughness, FadeIn, FadeOut
	},
	Movement = {
		Speed = 851,
		Delay = 12,
		Reversed = false
	},
	Rebounding = {
		Enabled = true,
		Type = "Ambush", -- "Blitz"
		Min = 5,
		Max = 5,
		Delay = 0.3
	},
	Damage = {
		Enabled = true,
		IgnoreHiding = false,
		Range = 40,
		Amount = 125
	},
	Crucifixion = {
        Type = "Guiding",
		Enabled = true,
		Range = 100,
		Resist = false,
		Break = true
	},
	Death = {
		Type = "Guiding", -- "Curious"
		Hints = {"¡¡ THIS SMILE IS LIKE AMBUSH !!", "Hide and don't Smile Don't get Tricked"},
		Cause = "Smiler"
	},
	Jumpscare = {
		Enabled = true, -- Set to true to show a jumpscare when the entity kills the player
		Image1 = "rbxassetid://11417375410", -- Shown during the tease (rbxassetid or a direct image URL)
		Image2 = "rbxassetid://11417375410", -- Shown during the scare (falls back to Image1 if empty)
		Sound1 = { Id = 5263560566, Properties = { Volume = 2.1 } }, -- Plays when the tease starts (only if Tease is enabled)
		Sound2 = { Id = 5263560566, Properties = { Volume = 2.1 } }, -- Plays at the scare
		Shake = true,
		Flashing = {
			Enabled = true,
			Color = Color3.fromRGB(255, 255, 255)
		},
		Tease = {
			Enabled = false,
			Min = 1,
			Max = 3
		},
		AspectRatio = 1, -- Image width / height (e.g. 16/9 for wide images, 1 for square)
		Scale = { -- Image HEIGHT as a multiple of the screen height
			Start = 0.7, -- Size when the scare begins
			End = 1.85 -- Size the scare grows to
		}
	}
})

---====== Debug entity ======---

entity:SetCallback("OnSpawned", function()
    print("Entity has spawned")
end)

entity:SetCallback("OnStartMoving", function()
    print("Entity has started moving")
end)

entity:SetCallback("OnEnterRoom", function(room: Model, firstTime: boolean)
    if firstTime == true then
        print("Entity has entered room: ".. room.Name.. " for the first time")
    else
        print("Entity has entered room: ".. room.Name.. " again")
    end
end)

entity:SetCallback("OnLookAt", function(lineOfSight: boolean)
	if lineOfSight == true then
		print("Player is looking at entity")
	else
		print("Player view is obstructed by something")
	end
end)

entity:SetCallback("OnRebounding", function(startOfRebound: boolean)
    if startOfRebound == true then
        print("Entity has started rebounding")
	else
        print("Entity has finished rebounding")
	end
end)

entity:SetCallback("OnDespawning", function()
    print("Entity is despawning")
end)

entity:SetCallback("OnDespawned", function()
    print("Entity has despawned")
end)

entity:SetCallback("OnDamagePlayer", function(newHealth: number)
	if newHealth <= 0 then
		print("Entity has killed the player")
	else
		print("Entity has damaged the player")
	end
end)

--[[

DEVELOPER NOTE:
By overwriting 'CrucifixionOverwrite' the default crucifixion callback will be replaced with your custom callback.

entity:SetCallback("CrucifixionOverwrite", function()
    print("Custom crucifixion callback")
end)

]]--

---====== Run entity ======---

entity:Run(true)
